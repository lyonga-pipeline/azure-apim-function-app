# Lean single Windows VM. NICs, managed data disks, extensions and domain-join
# are caller-owned and composed by their dedicated modules. Resource in
# virtual-machines.tf. Prefer terraform-azurerm-compeer-windows-vm for new work;
# both are maintained to the same standard.
