# Compeer Monitor Data Collection Endpoint

Reusable Azure Monitor Data Collection Endpoint module. It exposes the common endpoint properties, keeps tags/timeouts configurable, and returns composition-ready endpoint IDs and ingestion/configuration endpoints.
