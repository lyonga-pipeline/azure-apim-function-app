# Basic Management Locks

This example creates a `CanNotDelete` lock for a critical platform resource group.

Use locks as a separate composition decision in platform or workload patterns. Do not hide lock creation inside unrelated resource modules.
