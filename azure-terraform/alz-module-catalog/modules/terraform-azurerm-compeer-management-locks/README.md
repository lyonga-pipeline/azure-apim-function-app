# Compeer Management Locks

Applies Azure management locks to platform scopes. Use sparingly and only after the owning root module is stable because locks can block later changes if applied too broadly.
