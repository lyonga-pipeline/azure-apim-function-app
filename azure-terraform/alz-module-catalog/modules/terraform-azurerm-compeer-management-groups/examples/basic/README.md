# Basic Management Group Hierarchy

This example creates an ALZ management group hierarchy under an existing tenant/root management group and associates a subscription to the `corp` landing-zone group.

Replace the sample parent management group ID and subscription ID before running the example.
