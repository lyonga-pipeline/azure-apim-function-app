# Compeer Defender SOC Posture

Records and optionally deploys Defender for Cloud posture. The default `enabled = false` makes the module safe for no-cost foundation deployments while still documenting the intended SOC target state.

Enable only after cost approval, SOC design approval, and any import of subscription-level Defender settings that already exist outside Terraform.
