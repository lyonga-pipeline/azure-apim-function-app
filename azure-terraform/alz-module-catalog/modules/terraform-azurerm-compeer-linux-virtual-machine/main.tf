# Lean single Linux VM. NICs, managed data disks, extensions and domain-join are
# caller-owned and composed by their dedicated modules. Resource in
# virtual-machines.tf. See README.md for the lifecycle contract.
