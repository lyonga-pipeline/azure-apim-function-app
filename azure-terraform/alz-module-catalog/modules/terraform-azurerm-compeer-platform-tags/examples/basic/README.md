# Basic Platform Tags

This example emits the standard enterprise tag map used by platform and workload patterns.

The module does not enforce missing tags. OPA and Azure Policy enforce the final tag rules for landing-zone deployments.
