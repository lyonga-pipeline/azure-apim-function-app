/**  
*  # Azure Vnet module
*  
*  Module creates an Azure Vnet module with dynamic Subnets, Network Security Groups(NSG) and NSG rules.By default, this module will not create a resource group, proivde the name here to use an existing resource group, specify the existing resource group name, and set the argument to `create_resource_group = true`. Location will be same as existing RG.
*/